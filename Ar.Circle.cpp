#include <stdio.h>
int main()
{
    float a=3.14;
    float b,c;
    printf("Enter the value of radius of circle to calculate its Area:");
    scanf("%f",b);
    c=a*b;
    printf("The Area of Circle according to entered radius is: %f sq.units",c);
}
